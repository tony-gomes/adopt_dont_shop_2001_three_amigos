<section>
  <%= image_tag ( @pet.image ) %>
  <h2><%= @pet.name %></h2>
  Status: <%= @pet.adopt_status %><br/>

  if !favorite_check
    <%= link_to "Favorite Pet", "/favorites/#{@pet.id}", method: :patch %>
  elseif favorite_check
    <%= link_to "Remove Pet", "/favorites/#{@pet.id}", method: :delete %>
  end


  <p>About <%= @pet.name %> :</p>
  <p><%= @pet.description %></p>
  Age: <%= @pet.age %><br/>
  Sex: <%= @pet.sex %><br/>
  <p><%= link_to "Update Pet", "/pets/#{@pet.id}/edit" %>
  <%= link_to "Delete Pet", "/pets/#{@pet.id}", method: :delete %></p>
  <% flash.each do |name, msg| %>
  <div class="well_done"><%= msg %></div>
  <div class="favorite_removed"><%= msg %></div>
  <% end %>
</section>
