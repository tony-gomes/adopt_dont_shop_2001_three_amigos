<h2>Applications for <%= @pet.name %></h2>

<section>
  <% @pet.pet_applications.each do |application| %>
    <%= link_to application.name, "/pet_applications/#{application.id}" %>
  <% end %>
</section>
